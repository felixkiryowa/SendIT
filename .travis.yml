language: python
python:
  - "3.6.1"

services:
  - postgresql

# command to install dependencies
install:
  - pip install -r requirements.txt

# before_script:
#   - psql -c 'create database SendIT;' -U postgres
before_script:
  - psql -c "CREATE DATABASE SendIT;" -U postgres
  - psql -c "CREATE USER postgres WITH PASSWORD 'atagenda1@';" -U postgres
# command to run tests
script: 
- pytest --cov=.
after_success:
- coveralls